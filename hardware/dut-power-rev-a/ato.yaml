# atopile project — FirstLight FL-1 DUT Power + Fast-Trip Board Rev A
requires-atopile: ">=0.15.0"
paths:
  src: elec/src
  layout: elec/layout
builds:
  default:
    entry: elec/src/main.ato:DUTPowerBoard
