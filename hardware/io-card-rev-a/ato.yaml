# atopile project — FirstLight FL-1 Low-Speed Measurement and Utility I/O Card Rev A
requires-atopile: ">=0.15.0"
paths:
  src: elec/src
  layout: elec/layout
builds:
  default:
    entry: elec/src/main.ato:IoCard
