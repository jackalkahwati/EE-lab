# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local
# secret backups (.env.local.bak, .env.local.bak.google) — never commit
.env*.local.bak*

# Common ignores
node_modules
.next/
# isolated build output (NEXT_DIST_DIR=.next-build npm run build) — keeps a
# build from clobbering a running dev server's .next. MUST stay ignored: the
# Tailwind oxide scanner crashes on generated binaries in scanned paths.
.next-build/
.DS_Store
# pipeline run download artifacts
public/fab/
public/firmware/

# transient per-run debug report (regenerated every pipeline run)
public/data/last-run.json
public/data/last-run.md

# per-run artifact snapshots (regenerated each run)
public/runs/
data/

# generated 3D board models (kicad-cli glb export, 20-40 MB binaries —
# also excluded from Tailwind's source scanner, which crashes on them)
*.glb

# scratch run outputs (regenerated; never commit)
scripts/eda_runs/
scripts/flroute_runs/
# TS build cache
tsconfig.tsbuildinfo
# uploaded evidence files (user data)
data/evidence-artifacts/
# Next auto-generated type shim
next-env.d.ts
