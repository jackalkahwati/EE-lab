
.DS_Store
**/__pycache__/
tools/parts/registry.sqlite
tools/parts/.jlcparts-cache/
tools/parts/registry.sqlite-wal
tools/parts/registry.sqlite-shm

# tscircuit scratch outputs (dsn passes, drc runs)
tools/tscircuit/[0-9]*/

# Stray Onshape exports dropped at the repo root (real CAD lives under cad/ and hardware/).
# Root-only: does not affect .step files tracked in subdirectories.
/*.step

# golden suite scratch renders
tools/tscircuit/golden/.out-*
